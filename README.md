to clone the repo:
git clone <repo https>

to check the remote origins :
git remote -v

to add origins:
git remote add originn <repo https>

pull any updates:
git pull

see branches:
git branch -a

remove the branch from local:
git branch -d <branch name>
